version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd baseballapp
        - npm install
    build:
      commands:
        - npm install
        - npm run build
  artifacts:
    baseDirectory: /baseballapp
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*