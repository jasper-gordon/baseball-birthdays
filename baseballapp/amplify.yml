version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm install
        - npm run build
  artifacts:
    baseDirectory: /baseballapp
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*